/**
 * The application version.
 *
 * Declared here rather than read from `package.json` at runtime: the built `dist/` output does not
 * ship the manifest, and reading it would make the version a filesystem concern.
 */
export const APP_VERSION = "0.0.0";
